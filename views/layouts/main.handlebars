<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Just Tech News</title>

  {{!-- <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous"> --}}
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body class="bg-cabbage">
 
    <header class="bg-brown">
     <div class="wrapper">
      <nav >
        {{#if loggedIn}}
        <a class="font-cabbage" href="/dashboard">dashboard</a>
        <button id="logout" class="link-btn font-cabbage">logout</button>
        {{else}}
        <a class="font-cabbage" href="/login">login</a>
        {{/if}}
      </nav>

        <h1 class="text-center bg-cabbage font-brown">
       The Tech Blog
        </h1>

      </div>
    </header>
     <div class="wrapper">
    <main>
      {{{ body }}}
    </main>
    <footer>
    </footer>
  </div>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}

</body>

</html>